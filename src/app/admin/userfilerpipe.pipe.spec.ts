import { UserfilerpipePipe } from './userfilerpipe.pipe';

describe('UserfilerpipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserfilerpipePipe();
    expect(pipe).toBeTruthy();
  });
});
